<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pig.ui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IndexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".split.MainActivity" />
        <activity android:name=".canvas2.MainActivity" />
        <activity android:name=".animator.MainActivity"/>
        <activity android:name=".splashwangyi.SplashActivity"/>
        <activity android:name=".screenfit.dynamic.MainActivity"/>
        <activity android:name=".density.MainActivity"/>
        <activity android:name=".customer.CarActivity"/>
    </application>

</manifest>